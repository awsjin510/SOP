請從以下既有文件抽取 SOP 步驟。

**SOP 標題**：{{title}}
**適用對象**：{{target_audience}}
**素材檔名**：{{source_file}}
**檔案類型**：{{format}}（docx 已轉為純文字／pdf 已 OCR 為純文字）

---

```document
{{content}}
```

---

請直接輸出 JSON fenced block。
